# ---- data: NEVER commit the corpus. It is reproducible from scripts + manifest.
# Anchored with a leading slash: an unanchored `data/` also matched docs/data/, so
# the generated schema reference and the sources page were never committed.
/data/
# Orphan copy made when a notebook ran with CWD=notebooks/ (see analyst.config.ROOT).
notebooks/data/
*.pdf
*.parquet

# ---- secrets
.env
.env.local
*.pem

# ---- python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ---- os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
